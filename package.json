{
  "name": "@formance/formance-sdk-oauth",
  "version": "0.0.1",
  "description": "A helper library for usign Formance with OAuth2.0",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "tsc --build",
    "check:tsc": "tsc --noEmit --skipLibCheck",
    "check:eslint": "eslint --max-warnings=0 src",
    "check": "npm run check:tsc && npm run check:eslint"
  },
  "keywords": [],
  "author": "Formance",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "zod": ">= 3"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
